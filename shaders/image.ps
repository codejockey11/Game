#include "image.hlsl"
#include "Samplers.hlsl"

float4 main(SPixel vs) : SV_Target0
{
    float4 p = texture0.Sample(gsamLinearWrap, vs.uv);
	
    p.rgb *= m_ambient.rgb;
    p.rgb *= m_diffuse.rgb;

	return p;
}